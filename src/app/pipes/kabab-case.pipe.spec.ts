import { KababCasePipe } from './kabab-case.pipe';

describe('KababCasePipe', () => {
  it('create an instance', () => {
    const pipe = new KababCasePipe();
    expect(pipe).toBeTruthy();
  });
});
